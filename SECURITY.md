# Security policy

Do not open a public issue containing secrets or personal information. Report a vulnerability privately through GitHub's repository security-advisory feature.

The project intentionally has no browser-cookie ingestion, credential storage, broker connection, or autonomous external-action interface. Treat local input files as sensitive even though the software processes them locally.

