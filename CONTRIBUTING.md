# Contributing

Use a focused branch and include tests for behavior changes. Before proposing a change, run:

```sh
ruff check .
ruff format --check .
pytest -q
```

Do not add personal information, real resumes, credentials, browser state, proprietary question banks, or licensed market datasets. Changes that enable final application submission, active assessment completion, or trade execution are out of scope.

